# Firewall Ingress Rules

This directory contains Terraform configuration for managing ingress firewall rules.

## Usage
These rules should be applied to the shared VPC host projects to control incoming traffic.
