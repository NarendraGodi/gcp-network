# Firewall Egress Rules

This directory contains Terraform configuration for managing egress firewall rules.

## Usage
These rules should be applied to the shared VPC host projects to control outgoing traffic.
